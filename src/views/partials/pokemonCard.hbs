
<div class="col-md-3 mt-4">
  <div class="card w-80" >
    <img class="card-img-top" style="border: 2px solid #161616;" src="{{sprite}}" alt="{{name}}"/>
    
    <div class="card-body justify-content-center">
      
      <h4 class="card-title text-center"><b>{{name}}</b></h4>
      <p class="card-text text-center">Tipo: <i>{{type}}</i></p>
      
    	
    	<div class="d-flex justify-content-around mt-3">
    		<div class="row text-center flex-direction">
          <form action="/mispokemons/{{name}}" method="GET">
    			  <button class="btn btn-primary regular-button mr-2" type="submit">Ver</button>
    			</form>
          <form action="/borrarpokemon" method="POST">
            <button class="btn btn-danger regular-button ml-2" name="name" value="{{name}}" type="submit">Eliminar</button>
          </form>

        </div>
        
    	</div> 
    	    
    </div>
  </div>
</div>